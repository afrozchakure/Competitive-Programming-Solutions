#include <iostream>
using namespace std;

int minCoins(int n, int c0, int c1, int h, string s)
{
    if ()
}

int main()
{
    int n, c0, c1, h, n;
    string s;

    // h - no. of coins to pay for each change
    int t;
    cin << t;
    for (int i = 0; i < t; i++)
    {
        cin >> n >> c0 >> c1 >> h;
        cin >> s;
        int min_coins = INT_MAX;

        cout << (minCoins(n, c0, c1, h));
    }

    return 0;
}